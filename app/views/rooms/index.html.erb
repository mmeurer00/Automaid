<h1> Rooms </h1>
<p> hello <%= current_user.name %> </p>
<% @rooms.each do |room| %>
    <%= link_to room.name, room_path(room)%>
    <br>
    <% end%>

    <%= button_to 'LOGOUT', logout_path, class: 'nav-link active', method: 'delete' %>