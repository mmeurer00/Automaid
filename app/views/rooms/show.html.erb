<h1> <%= @room.name %> </h1>
<p> Indoor: <%= @room.indoor %> </p>
<p> Outdoor: <%= @room.outdoor %> </p>
<% @room.devices.each do |device|%>
    <p> <%= link_to device.name, device_path(device)%> </p>
    <% end %>